app:
  name: "mirim-bot"
  image: "ghcr.io/invasor-de-fronteiras/mirim_bot:sha-759fcc7"
  replicas: 1

config:
  env: mirim-bot-secrets

network:
  expose: false

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 1