events {
    worker_connections 1024;
}

http {

    server {
        location /assets {
          alias /data/assets;
          autoindex on;
        }

        location /health {
            access_log off;
            add_header 'Content-Type' 'text/plain';
            return 200 "healthy\n";
        }
    }

}